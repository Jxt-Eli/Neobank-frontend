import React from "react";
import { View, ScrollView, Text, TextInput } from 'react-native'

export default function Transaction() {
    return (
        <View>
            <Text>check previous transactions from all sources</Text>
        </View>
    )
}